{
  "name": "my-app",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  {
  "scripts": {
    "build": "vite build",
    "preview": "vite preview",
    "dev": "vite",
    "deploy": "netlify deploy --prod"
  }
}
{
  "dependencies": {
    "react": "^18.2.0", // Use React 18 for stability
    "react-dom": "^18.2.0",
    "react-router-dom": "^6.14.1"
  },
  "devDependencies": {
    "@vitejs/plugin-react": "^4.3.4", // Use compatible version for Vite 5
    "vite": "^5.0.0", // Ensure compatibility with plugin-react
    "gh-pages": "^6.3.0"
  }
}

}

