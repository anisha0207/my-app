{
  "name": "my-app",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  {
  "scripts": {
    "build": "vite build",
    "preview": "vite preview",
    "dev": "vite",
    "deploy": "netlify deploy --prod"
  }
}

  "dependencies": {
    "react": "^19.0.0",
    "react-dom": "^19.0.0",
    "react-router-dom": "^7.3.0"
  },
  {
  "devDependencies": {
    "@vitejs/plugin-react": "^5.0.0-beta.8", // Use latest beta if v5 isn't released
    "vite": "^6.2.0",
    "gh-pages": "^6.3.0"
  }
}
}

